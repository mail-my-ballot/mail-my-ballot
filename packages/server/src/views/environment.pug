extends layout.pug

block pageName
  title Environment Variables

block content
  .container.mt-4
    h1 Environment Variables

  .container.mt-4
    ul
      each val, key in env
        li= key + ': ' + val
